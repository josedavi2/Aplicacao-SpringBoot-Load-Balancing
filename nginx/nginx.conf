events { worker_connections 1024; }
http {
    upstream myapps {
        server sistema-restaurante:8080;
    }
    server {
        listen 80;
        location / {
            proxy_pass http://myapps;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}